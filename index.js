/**
 * @format
 */

import {AppRegistry} from 'react-native';
import RecipeListScreen from './src/index';
import {name as appName} from './app.json';
import { AppRecetas } from './App';

AppRegistry.registerComponent(appName, () => AppRecetas);
